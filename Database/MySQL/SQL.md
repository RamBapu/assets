# SQL

### Questions

- SQL
- Types of SQL commands - DDL, DML, DCL, TCL
- primary key in SQL
- foreign key
- delete vs truncate
- joins - inner, left, right, full outer
- NULL value in SQL
- UNIQUE key
- primary vs UNIQUE constaints
- SQL (RDBMS) vs NoSQL
- constraints types - Primary key, foreign key, UNIQUE, NULL, DEFAULT
- normalization
- UNION vs UNION ALL
- indexes
- GROUP BY clause
- ORDER BY clause
- HAVING vs WHERE clause
- view
- stored procedure
- SQL Trigger
- aggregate functions - SUM, MIN, MAX, AVG, COUNT
- self join
- subquery
- transaction
- ACID properties
- SQL data types
- clustered index
- database schema
- deadlock problem

### Types of SQL commands

Data Definition language - ALTER, CREATE, DROP, TRUNCATE

Data Manipulation language - SELECT, INSERT, DELETE

Data Control language - GRANT, REVOKE

Transaction Control language - COMMIT, ROLLBACK

### UNION vs UNION ALL

Union doesn't has the duplicate records in the single combined table. While Union all would have
